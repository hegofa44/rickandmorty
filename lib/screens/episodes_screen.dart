import 'package:flutter/material.dart';

class EpisodeScreen2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Episodios'),
      ),
    );
  }
}
